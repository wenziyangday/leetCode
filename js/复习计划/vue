vue:
  组件传参：
    父子组件传参：
      正向参数：父到子，子组件定义接收名称和类别以及默认值
      逆向参数：子到父，子组件中触发方法（关键点emit）抛出给父级，父级绑定后获取子组件数据
      直接调用子组件的方法和参数：ref

    兄弟组件（跨层级组件之间的通信）：
      eventBus传输：全局注册一个$eventBus, 通过一个抛出（this.$eventBus.$emit），一个接收(this.$eventBus.$on)

      拥有相同父级的组件之间的传输：可以通过this.$parent.$emit()抛出; 通过this.$parent.$on 接收

      tips：
        callback 函数 应该使用箭头函数（保证this指向）

    参数：常量，变量，函数

  mixins:
    共享数据结构，以及生命周期，方法，但是不共享数据
    一般用于处理具有相同逻辑的业务

  slot：
    默认插槽
    具名插槽
    作用域插槽：可以通过设置，获取插槽的内部的数据变量

  vuex:
    全局注册后使用，
    可以通过this.$store 使用，也可以通过vuex伴生方法 mapGetters,等等使用

  指令：
    指令的职责是，当表达式的值改变时，将其产生的连带影响，响应式地作用于 DOM


