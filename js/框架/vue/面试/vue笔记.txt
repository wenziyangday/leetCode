1.vue 在实例化时，data 的什么选择一个函数，而非一个对象。
  a）对象本身存在地址引用问题
  b）无论通过实例对象修改属性值，还是通过对象修改属性值，本质上都是对相同引用地址下的数据进行修改
  c）而使用函数，可以避免这种地址引用指向问题：
    aa）调用函数会创建独立的函数作用域，调用结束销毁（互不影响）
    bb）每次调用函数都会创建一个新的函数作用域，且相互独立
  d）多个vue实例使用函数作为data，理由是一样的

2.生命周期中尽量避免使用箭头函数，这样会引发this的指向问题
