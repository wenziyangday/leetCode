1.声明式渲染
2.声明式渲染是建立在响应式的基础上的（响应式：建立了数据和dom之间的关联）
3.指令系统 v- 自定义指令：局部指令，全局指令 bind, inserted, update
4.组件系统--拆分技巧
5.响应式系统实现（响应式：数据和视图绑定，当数据更新后，视图就进行响应）
6.生命周期钩子函数：钩子函数的执行顺序，父子组件的钩子函数执行顺序，钩子函数之间的异同
  created、mounted 却别在于el是否生成
7.在底层的实现上，Vue 将模板编译成虚拟 DOM 渲染函数。结合响应系统，Vue 能够智能地计算出最少需要重新渲染多少组件，并把 DOM 操作次数减到最少。
8.JavaScript表达式 指的是：必须是单个表达式，否则 表达式不生效
9.指令的职责是 当表达式的值改变时，将其产生的连带影响，响应式地作用于 DOM。
10.计算属性和methods的区别
  计算属性是基于他们的响应式依赖进行缓存的，只在相关响应式依赖发生改变时它们才会重新求值。
  计算属性 不能进行异步操作 （需要验证 TODO）
11.侦听属性 watch
  需要在数据变化时执行异步或开销较大的操作
12.v-for 遍历对象
13. TODO 由于 JavaScript 的限制，Vue 不能检测数组和对象的变化
14.双向绑定v-model的实现
15.组件注册，局部注册和全局注册
16.插槽 具名插槽
17.依赖注入：提升了耦合度
18.mixin
19.自定义指令（局部指令，全局指令）
20.vue 更新dom是异步的
  只要侦听到数据变化，Vue 将开启一个队列，并缓冲在同一事件循环中发生的所有数据变更。
  如果同一个 watcher 被多次触发，只会被推入到队列中一次。
  这种在缓冲时去除重复数据对于避免不必要的计算和 DOM 操作是非常重要的。
21.数据变化后，需要立即更新dom 此时可以使用Vue.$nextTick()
22.mixins 中异步获取的数据值，无法直接在mounted、created 中使用 -- 无论mixin是一个函数还是一个对象
  疑问：mixin 中的异步数据，和组件method 中加载顺序是什么样的？
  将mixin中的生命周期和组件中的生命周期进行合并，然后根据正常的生命周期顺序进行执行，对于相同的生命周期（mixin 优于组件的），但是对于异步是放在队尾进行加载的
23.生命周期的顺序是如何保持顺序的。TODO
24.mixin 中的异步时序问题
