1.自定义指令
  局部自定义指令
    组件内部的
  全局自定义指令
    main 里面自定义的（app.use---app 指的是全局vue实例）

  自定义指令接收的参数（binding.value）
    对象
    函数
    字符串
  修饰符（modifiers）（v-demo:m.mm）其中mm就是修饰符，而m 是参数

2.setup 中进行 props（响应式对象）解构处理 会出现响应式失效
    此时你可以使用toRef，toRefs使其保持响应式

  在模板中使用ref值时为啥不需要添加value了？
    模板中访问ref值时，vue会进行自动浅层解包，所以无需在模板中使用.value

  setup() 自身并不含对组件实例的访问权，即在 setup() 中访问 this 会是 undefined。
    你可以在选项式 API 中访问组合式 API 暴露的值，但反过来则不行

  setup() 应该同步地返回一个对象。
    唯一可以使用 async setup() 的情况是，该组件是 Suspense 组件的后裔。

  使用了 <script setup>的组件是默认私有的：
    一个父组件无法访问到一个使用了 <script setup> 的子组件中的任何东西，
      除非子组件在其中通过 defineExpose 宏显式暴露：

  <script setup> 出现的原因：
    解决通过setup() 函数中手动暴露大量的状态和方法非常繁琐
    <script setup> 中的顶层的导入、声明的变量和函数可在同一组件的模板中直接使用

3.provide，inject，跨组件共享（异步网络请求--重复处理），但是跨页面却不会出现

4.reactive 直接进行整个对象 替换（修改），会使该对象的响应式丢失
  reactive() 将深层地转换对象：当访问嵌套对象时，它们也会被 reactive() 包装。
  当 ref 的值是一个对象时，ref() 也会在内部调用它。

  ref 创建的简单对象，进行整体操作不会出现响应式丢失问题

  为什么使用ref？（基于响应式）
    在标准的 JavaScript 中，检测 普通变量 的访问或修改是行不通的。
    然而，我们可以通过 getter 和 setter 方法来拦截对象属性的 get 和 set 操作。
    另一个 ref 的好处是，与普通变量不同，你可以将 ref 传递给函数，同时保留对最新值和响应式连接的访问。

  ref的解包行为：
    作为 reactive 对象的属性
      一个 ref 会在作为响应式对象的属性被访问或修改时自动解包。换句话说，它的行为就像一个普通的属性：
      只有当嵌套在一个深层响应式对象内时，才会发生 ref 解包。当其作为浅层响应式对象的属性被访问时不会解包。

    在模板渲染上下文中，只有顶级的 ref 属性才会被解包。

  reactive局限性：建议使用ref作为响应式的声明
    有限的值类型 只能用于对象类型，不能针对基本数据类型

    不能替换整个对象：
      由于 Vue 的响应式跟踪是通过属性访问实现的，因此我们必须始终保持对响应式对象的相同引用。
      这意味着我们不能轻易地“替换”响应式对象，因为这样的话与第一个引用的响应性连接将丢失

    对解构操作不友好：
      当我们将响应式对象的原始类型属性解构为本地变量时，或者将该属性传递给函数时，我们将丢失响应性连接

    疑问？
      响应式数据（数组响应式--ref、reactive）使用splice 丢失响应式？
      场景：
        先移除数据、后全量替换 响应式丢失
        不会丢失响应式，但是要注意对于默认数据是一个复杂数据类型的时候要注意 进行 拷贝（深、浅拷贝）

      关于复杂数据类型作为变量（A）传入 响应式ref、reactive中，本质上是将A的引用传递给ref、reactive 因此对于ref、reactive执行的操作，本质上是对
      A的数据进行操作

5.h 函数，createVNode()

6.解构 影响响应式的追踪？

7.props/emits（defineProps/defineEmits） 触发方式在子组件内部
  当触发方式在子组件外部的时候 refs，provide/inject

8.symbol 作为基础数据类型，可以在vue页面上显示，而作为引用类型的键会不显示，至于作为值还没有校验？

9.组件、页面、hooks 几种之间的差异

10.watch 监听一个响应式对象的时候，新旧值是一样的

11.DOM更新时机
  当你修改了响应式状态时，DOM 会被自动更新。但是需要注意的是，DOM 更新不是同步的。
  Vue 会在“next tick”更新周期中缓冲所有状态的修改，以确保不管你进行了多少次状态修改，每个组件都只会被更新一次。

12.计算属性 有缓存机制
  Getter 不应有副作用
  避免直接修改计算属性值（默认只读的，只有在特殊的场景下，可以通过提供getter和setter来操作）

13.watchEffect
  允许我们自动跟踪回调的响应式依赖（解决变化数据同时作为使用数据）
  侦听一个模板引用ref 的变化

14.ref 可以是一个字符串、也可以是一个函数

15.动态组件
  <component :is='com'/>
  com 可以是一个被注册的组件名，也可以是一个 导入的组件对象

16.inject、provide需要在setup中进行 同步 调用（或者在<script setup>中调用）

17.defineAsyncComponent：仅在需要时再从服务器加载相关组件

18.组合式函数（与mixins区别）
    不清晰的数据来源
    命名空间冲突
    隐式的跨 mixin 交流

19.自定义指令
  自定义指令主要是为了重用涉及普通元素的底层 DOM 访问的逻辑。
  指令执行的时间：
    beforeCreated 和 onMounted 之间

20.teleport
  <Teleport> 挂载时，传送的 to 目标必须已经存在于 DOM 中。
  理想情况下，这应该是整个 Vue 应用 DOM 树外部的一个元素。
  如果目标元素也是由 Vue 渲染的，你需要确保在挂载 <Teleport> 之前先挂载该元素。

21.相比于 Vuex，
  Pinia 提供了更简洁直接的 API，
  并提供了组合式风格的 API，
  最重要的是，在使用 TypeScript 时它提供了更完善的类型推导。

22.性能优化
  页面加载性能：首次访问时，应用展示出内容与达到可交互状态的速度
    最大内容绘制（LCP）
    首次输入延时（FID）

  更新性能：应用响应用户输入更新的速度

23.运行声明

24.setup 中的父子组件的执行顺序
  当使用 <script setup> 的时候，任何在 <script setup> 声明的顶层的绑定 (包括变量，函数声明，以及 import 导入的内容) 都能在模板中直接使用

25.WeakMap
  必须以对象作为键
  键为弱键，即作为键的对象被删除或者被重新赋值则键值对会同时消失
  由于键是弱键，导致无法判断此时此刻，彼时彼刻任一键值对是否存在，导致没有迭代器

26. !. 这是TypeScript的语法，叫非空断言操作符（non-null assertion operator），
    和?.相反，这个符号表示对象后面的属性一定不是null或undefined。

27.响应式机制

28.diff

29.调度机制

30. 地图layer amap

31.defineComponent和defineProps都是Vue 3中用于定义组件的函数，但它们的作用不同。
   defineProps是Vue 3中提供的一个API，用于定义组件的props。
   defineComponent是Vue 3中提供的一个高阶函数，用于定义组件，它不仅支持props，还支持其他的组件选项，如data、computed、methods等。
   因此，defineProps只是定义组件props的函数，而defineComponent则是定义组件的高阶函数。

32.defineComponent和script setup都可以定义组件，但它们在用法和功能上存在差异。
   defineComponent是Vue 3中用于定义组件的函数，它接收一个对象作为参数，包含组件的各种配置选项，例如组件的模板、数据、方法等。
   而script setup是Vue 3中引入的新特性，允许在单个文件中定义组件的数据、属性和方法，而不需要像Vue 2那样分别使用data、props、methods等选项来定义。
   因此，虽然两种方式都可以用来定义组件，但使用script setup可以更加简洁地定义组件。

33.路由懒加载和异步组件
  异步组件：是为另一个组件提供的包装器，来让被包装的组件可以进行懒加载。
    这通常用作减少构建后的 .js 文件大小的一种方式，通过将它们拆分为较小的块来按需加载。
  路由懒加载：当打包构建应用时，JavaScript 包会变得非常大，影响页面加载。如果我们能把不同路由对应的组件分割成不同的代码块，
    然后当路由被访问的时候才加载对应组件，这样就会更加高效。



