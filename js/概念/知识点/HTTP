HTTP：
  1.报文结构
  2.请求方式
  3.状态码
    1）1XX 协议处理的中间状态、还需要后续操作
    2）2XX 成功状态
    3）3XX 重定向状态，资源位置发生变动，需要重新请求。
    4）4XX 请求报文有误
    5）5XX 服务端发生错误
  4.URI（统一资源标志符） = URL+URN
  5.HTTP特点
    1）灵活可扩展
    2）可靠传输   主要是TCP自身特性
    3）请求-应答（一发一收、有来有回）
    4）无状态 由于http请求时独立的、无关的，不需要进行状态信息保留

    缺点：
      无状态
      明文传输
      对头阻塞问题
  6.Accept 系列
    数据格式
    压缩方式
    支持语言
    支持字符集
  7.定长和不定长包体
    定长包体：Content-length 进行传输长度限制
    不定长：Transfer-Encoding: chunked 表示分块传输
  8.对头阻塞问题？
    HTTP传输是基于请求-应发的模式进行的，报文必须是一发一收
    在一个任务队列中串行执行时，当队首请求处理结束过慢，就会产生堵塞
    解决方案：
      并发连接
      域名分片

  9.Cookie
    是对于无状态的http的丰富
    用于存储HTTP的中间状态

    属性：
    1）生存周期：Expire/max-age
    2）作用域:domain/path
    3）安全相关: httpOnly、secure、sameSite

    缺点：
    1）容量
    2）性能 每个请求都会携带完整的cookie，请求越多，cookie越大
    3）安全缺陷


