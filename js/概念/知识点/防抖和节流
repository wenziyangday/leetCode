节流：
  核心思想：
    在定时器的时间范围内再次触发，则不予理睬，等到当前定时器完成，才能启动下一个定时器任务。
    eg:公交车，10min 一趟，10min 好、中有多少人在公交站等 我不关心， 10分钟一到,我就发车


防抖：
  核心思想：
    每次事件触发则删除原来的定时器，建立新的定时器。
    eg:和LOL中的回城功能类似，反复触发回城功能，那么只认最后一次，从最后一次触发开始计时
